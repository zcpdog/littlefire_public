<div class="timeline-marker">
    <span><%=year_month%></span>
</div>
<div id="timeline-container">
	<%objs.each do |deal|%>
	    <div class="activity <%= cycle("left", "right") -%>">
	        <div class="line-item yui3-g-r">
	            <div class="line-content">
	                <div class="yui3-u-1-4">
					   	<%=image_tag(deal.picture.image.url(:thumb), width: 80) if deal.picture.present?%>
						<%=holder_tag 80,"picture","sky" unless deal.picture.present?%>
	                </div>
	                <div class="yui3-u-3-4">
					   <%=link_to deal.title, deal_path(deal)%>
	                </div>    
	            </div>
	        </div>
	        <i></i> 
	    </div>
	<%end%>
</div>